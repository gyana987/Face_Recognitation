[DEFAULT]
; DEVICE matches real AI_VMS's convention: a GPU index digit (e.g. "0" -> cuda:0,
; falling back to cpu if CUDA isn't available), or the literal string "cpu".
DEVICE = 0
PERSON_CONF = 0.25
PERSON_IMGSZ = 1280
; Ultralytics' bundled ByteTrack config -- used when a caller passes
; metadata={"tracking": True} (see Detection/YOLO/inference.py run()).
TRACKER = bytetrack.yaml
